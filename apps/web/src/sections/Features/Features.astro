---
import {
  Card,
  CardContent,
  CardDescription,
  CardFooter,
  CardHeader,
  CardTitle,
} from '@avador/ui/card/card';
import { ShieldCheck, Tag, Truck } from 'lucide-react';
import styles from './Features.module.css';
---

<section class={styles.featuresSection}>
  <div class={styles.container}>
    <h2 class={styles.title}>¿Por qué elegir Avador 1842?</h2>
    <div class={styles.cardsGrid}>
      <Card>
        <CardHeader className={styles.cardHeader}>
          <ShieldCheck size={28} className={styles.icon} />
          <CardTitle>Fragancias 100% originales</CardTitle>
          <CardDescription>Calidad garantizada, sin imitaciones.</CardDescription>
        </CardHeader>
        <CardContent>
          <p>
            Trabajamos únicamente con productos auténticos, seleccionados cuidadosamente. Sin
            sorpresas, sin copias.
          </p>
        </CardContent>
        <CardFooter>
          <a href="/home" class={styles.link}>Explorar catálogo →</a>
        </CardFooter>
      </Card>

      <Card>
        <CardHeader className={styles.cardHeader}>
          <Truck size={28} className={styles.icon} />
          <CardTitle>Envíos rápidos y seguros</CardTitle>
          <CardDescription>Recibí tu pedido sin complicaciones.</CardDescription>
        </CardHeader>
        <CardContent>
          <p>
            Hacemos que cada entrega llegue perfecta. Enviamos con seguimiento y empaques bien
            cuidados.
          </p>
        </CardContent>
        <CardFooter>
          <a href="/home" class={styles.link}>Ver más detalles →</a>
        </CardFooter>
      </Card>

      <Card>
        <CardHeader className={styles.cardHeader}>
          <Tag size={28} className={styles.icon} />
          <CardTitle>Precios accesibles</CardTitle>
          <CardDescription>Perfumes de calidad al mejor valor.</CardDescription>
        </CardHeader>
        <CardContent>
          <p>
            Acercamos fragancias premium a precios justos. Queremos que puedas elegir tu aroma sin
            pagar de más.
          </p>
        </CardContent>
        <CardFooter>
          <a href="/home" class={styles.link}>Conocer más →</a>
        </CardFooter>
      </Card>
    </div>
  </div>
</section>
