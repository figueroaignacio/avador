---
import { Button } from '@avador/ui/button/button';
import styles from './Hero.module.css';
---

<section class={styles.hero}>
  <div class={styles.content}>
    <h1 class={styles.title}>Avador 1842 – Encontrá tu esencia, encontrá tu aroma perfecto.</h1>
    <h2 class={styles.subtitle}>Próximamente, desde Monte Grande, Bs. As. Argentina</h2>
    <div class={styles.buttons}>
      <Button variant="primary">Explorar Catálogo</Button>
      <Button variant="secondary">Conoce más sobre nosotros</Button>
    </div>
  </div>
</section>
